#include <iostream>
using namespace std;

int main() {
    // Define an array of short integers and initialize it with values.
    short numbers[] = {10, 20, 30, 40, 50};

    // Access and print the first element of the array using the * operator.
    cout << "The first element of the array is " << *numbers << endl;

    return 0;
}
